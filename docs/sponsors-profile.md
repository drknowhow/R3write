# GitHub Sponsors profile copy

Source-of-truth for the text that lives on
**https://github.com/sponsors/drknowhow**.

GitHub's API has no `updateSponsorsListing` mutation, so any change to
the profile must be made through the dashboard UI:

1. Sign in as `drknowhow`.
2. Open **https://github.com/sponsors/drknowhow/dashboard/profile**
3. Paste the two blocks below into the matching fields and click
   **Update profile** at the bottom.

Keep this file in sync whenever the wording is updated so the next paste
is one copy away.

---

## Short bio

> Paste into the **Short bio** field (one-line, ~150 char limit).

```text
Heavy AI power user & creator of R3write (inline AI rewrite for Windows), Sentinel (AI-powered browser testing), and C3 (Code Context Control). I build local-first tools that close the gap between AI potential and actual workflow efficiency.
```

---

## Description

> Paste into the long **Description** field. Markdown is rendered.

```markdown
## What I build

**[R3write](https://github.com/drknowhow/R3write)** — Inline AI rewrite for Windows. Select text in any app, press `Ctrl+Alt+G`, pick an action, paste the result back. Free local Ollama by default; bring your own key for Gemini, OpenAI, Anthropic, Groq, OpenRouter, or Ollama Cloud. Local-first, no telemetry, no R3write server — your keys and text never pass through anything I control.

**Sentinel** — AI-powered browser testing.

**C3 (Code Context Control)** — surgical code context for LLM coding agents.

All three are local-first by design.

## Why sponsor

Your support funds:

- **Free public betas** — R3write is free during beta. Sponsorship keeps it free while it's still finding its shape.
- **New features over monetisation** — the to-build list is long (caret-anchored popup positioning, per-action model overrides, A/B regenerate, recipe chaining, eventual macOS support). Sponsors push items up the queue.
- **Independent maintenance** — no VC, no growth metrics, no dark patterns. Just tools that do one thing well.

## Other ways to support

Prefer a one-time tip? [Buy me a coffee →](https://buymeacoffee.com/drknowhow)
```
